package Hello::Test;
    
    use strict;
    use warnings;
    
    use base 'Mojolicious::Controller';
    
    sub me {
        my $self = shift;

        $self->render(text => 'The me method from controller test is called!');
      }

    1;
